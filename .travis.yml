language: java

jdk:
  - oraclejdk8

install: false

script:
  - mvn package -f maven-sdk3/pom.xml
  - ./gradle-with-plugin/gradlew -b gradle-with-plugin/build.gradle assemble

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
